{
  "name": "ucsd-graduation-planner",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "npm install --prefix ./mern/client && npm run build --prefix ./mern/client",
    "start": "npm run preview --prefix ./mern/client",
    "dev": "npm run dev --prefix ./mern/client",
    "install-client": "npm install --prefix ./mern/client",
    "postinstall": "npm run install-client"
  },
  "dependencies": {
    "@dnd-kit/core": "^6.3.1",
    "@dnd-kit/utilities": "^3.2.2",
    "googleapis": "^150.0.1",
    "tailwind-scrollbar": "^4.0.2"
  }
}
